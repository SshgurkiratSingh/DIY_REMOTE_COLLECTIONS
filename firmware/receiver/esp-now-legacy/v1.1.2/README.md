# ESP-NOW Legacy Receiver v1.1.2

## Purpose
Legacy receiver with structured reply telemetry support.

## Build/Upload
```bash
cd firmware/receiver/esp-now-legacy/v1.1.2
pio run
pio run --target upload
```

## Notes
Useful when validating callback and response packet behavior before moving to direct motor receivers.
