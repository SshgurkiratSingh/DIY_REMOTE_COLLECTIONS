# ESP-NOW Legacy Receiver v1.1

## Purpose
Earliest lightweight receiver pipeline.

## Build/Upload
```bash
cd firmware/receiver/esp-now-legacy/v1.1
pio run
pio run --target upload
```

## Notes
- One-way receive focused.
- Good for packet-format validation.
