# Direct BTS Receiver v1.0

## Purpose
Simple direct receiver for early motor-control integration.

## Build/Upload
```bash
cd firmware/receiver/direct-bts/v1.0
pio run
pio run --target upload
```

## Notes
Baseline implementation for serial + command-path verification.
