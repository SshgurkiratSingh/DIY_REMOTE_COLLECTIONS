# Direct BTS Receiver v2.0

## Purpose
Main direct-drive receiver with dual motor output and runtime command handling.

## Build/Upload
```bash
cd firmware/receiver/direct-bts/v2.0
pio run
pio run --target upload
```

## Notes
Recommended direct receiver for non-IMU motor projects.
