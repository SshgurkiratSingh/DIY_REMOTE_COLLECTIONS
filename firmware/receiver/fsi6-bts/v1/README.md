# FS-i6 BTS Receiver v1

## Purpose
Baseline FS-i6 RC to BTS motor bridge receiver without IMU layer.

## Build/Upload
```bash
cd firmware/receiver/fsi6-bts/v1
pio run
pio run --target upload
```

## Notes
Use when you need simple RC-driven bridge control.
