# Firmware

Central home for all firmware projects.

## Structure
- `transmitter/`: handheld/controller-side firmware.
- `receiver/`: target-side firmware variants.

## Build Tip
Each version folder is an independent PlatformIO project. Run commands inside the target version folder.
